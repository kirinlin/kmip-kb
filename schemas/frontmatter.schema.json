{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/kmip-dev/schemas/frontmatter.schema.json",
  "title": "KMIP KB document front matter",
  "description": "YAML front-matter contract for every knowledge-base document. Validated by scripts/build_kb_scaffold.py --check.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "title",
    "category",
    "spec_version",
    "spec_versions",
    "source_section",
    "status",
    "related",
    "keywords"
  ],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable document title (the spec section title)."
    },
    "category": {
      "type": "string",
      "enum": [
        "operation",
        "attribute",
        "object",
        "concept",
        "encoding",
        "enumerations",
        "structures",
        "messages",
        "profile",
        "reference",
        "workflow",
        "example",
        "schema",
        "index",
        "usage-guide"
      ],
      "description": "Which knowledge-base section this document belongs to."
    },
    "spec_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$",
      "description": "Baseline KMIP version this stub was generated from (e.g. \"2.1\")."
    },
    "spec_versions": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[0-9]+\\.[0-9]+$"
      },
      "uniqueItems": true,
      "description": "KMIP versions in which this concept appears. Populated during authoring."
    },
    "source_section": {
      "type": "string",
      "description": "Dotted v2.1 (baseline) spec section number for traceability ONLY (e.g. \"6.1.8\"). Never copy spec prose. Empty string allowed for non-section docs (index pages). The sentinel \"del_v2\" marks a doc whose feature was removed in v2.0 — its last-present section lives in v1_source_section."
    },
    "v1_source_section": {
      "type": "string",
      "description": "Dotted v1.x spec section number for the same concept, when it existed in the 1.x family (e.g. \"3.35\"). Optional: omitted for features introduced in v2.x, which have no v1 mapping."
    },
    "status": {
      "type": "string",
      "enum": ["stub", "draft", "reviewed"],
      "description": "stub = generated skeleton, prose pending; draft = authored, needs review; reviewed = human-verified per CONTRIBUTING checklist."
    },
    "related": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "description": "Slugs or titles of related KB documents (cross-references)."
    },
    "keywords": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "description": "Retrieval keywords for RAG / search."
    },
    "tag_hex": {
      "type": "string",
      "pattern": "^[0-9A-Fa-f]{6}$",
      "description": "6-digit uppercase hex tag value from KMIP §11.56 Tag Enumeration (e.g. \"42000D\"). Present only on docs that map to a named KMIP tag."
    },
    "xml_text": {
      "type": "string",
      "minLength": 1,
      "description": "CamelCase XML text identifier for this tag (e.g. \"BatchCount\"). Used as the XML element name for structure fields and as the enumeration value text for enumeration tags. Derived from the tag name per KMIP-ENCODE §6.1.3 normalisation algorithm."
    },
    "tag_type": {
      "type": "string",
      "enum": [
        "Structure",
        "Integer",
        "Long Integer",
        "Big Integer",
        "Enumeration",
        "Boolean",
        "Text String",
        "Byte String",
        "Date-Time",
        "Interval",
        "Date-Time Extended"
      ],
      "description": "TTLV type byte for this tag — what encoding the tag's value uses on the wire (e.g. \"Structure\", \"Enumeration\", \"Text String\"). Populated by scripts/populate_tag_type.py. Omitted for tags whose type is context-dependent (polymorphic)."
    }
  }
}
